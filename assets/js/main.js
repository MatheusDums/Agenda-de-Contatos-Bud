/* let botao_altera = document.querySelector(".contact_btn");
let container_form = document.querySelector("#form_container");

function showForm() {
  let classes_form = container_form.classList;
  if (classes_form.contains("hide")) {
    classes_form.classList.remove("hide");
    classes_form.classList.add("enable");
    botao_altera.innerHTML = "Fechar";
  } else {
    classes_form.classList.remove("enable");
    classes_form.classList.add("hide");
    botao_altera.innerHTML = "Adicionar Contato";
  }
} */

let btn_edita = document.querySelector('.edita_btn');
let formulario = document.querySelector('#form_container')

btn_edita.addEventListener('click', function() {

})